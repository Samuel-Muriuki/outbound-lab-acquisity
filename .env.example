# ============================================
# OUTBOUNDLAB ENVIRONMENT VARIABLES (FREE-TIER)
# ============================================
# Copy to .env.local and fill in real values.
# NEVER commit .env.local — secrets only.
# All providers below are FREE TIERS — no payment method required.

# ============================================
# DATABASE — Supabase (free tier, Singapore region)
# ============================================
# Dashboard → Project Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key

# Dashboard → Project Settings → API → service_role (KEEP SECRET — server only)
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key

# Optional: direct Postgres URL for migrations
DATABASE_URL=postgresql://postgres.PROJECT-REF:PASSWORD@aws-0-ap-southeast-1.pooler.supabase.com:6543/postgres

# ============================================
# LLM PROVIDER CHAIN — ALL FREE TIER
# ============================================
# Order: Groq → Gemini → OpenRouter (configured in src/lib/agents/llm/chat.ts)
# At least ONE must be set. All three is recommended for resilience.

# 1. PRIMARY: Groq — fastest inference (~500 tok/s for Llama 3.3 70B)
#    Sign up: https://console.groq.com
#    Free tier: 14,400 req/day, 30 req/min, 6,000 tokens/min
GROQ_API_KEY=gsk_your_groq_key_here

# 2. FALLBACK: Google Gemini — also used for embeddings (Phase 2)
#    Sign up: https://aistudio.google.com/app/apikey
#    Free tier: 1,500 req/day for gemini-2.5-flash
GEMINI_API_KEY=AIza_your_gemini_key_here

# 3. LAST-RESORT FALLBACK: OpenRouter — routes to free Llama hosts
#    Sign up: https://openrouter.ai
#    Free tier: ~50 req/day on free models (variable)
OPENROUTER_API_KEY=sk-or-v1-your_openrouter_key_here

# ============================================
# SEARCH TOOL — Tavily (free 1k/mo)
# ============================================
# https://tavily.com — built specifically for LLM agents
TAVILY_API_KEY=tvly-your-tavily-key-here

# ============================================
# RATE LIMITING (Phase 3) — optional
# ============================================
# Upstash Redis — https://console.upstash.com (free 10K commands/day)
# Leave commented for Phase 1-2; Postgres rate_limits table handles it
# UPSTASH_REDIS_REST_URL=https://your-instance.upstash.io
# UPSTASH_REDIS_REST_TOKEN=your-upstash-token

# ============================================
# MONITORING (Phase 3) — optional
# ============================================
# Sentry — https://sentry.io (free 5K errors/month)
# NEXT_PUBLIC_SENTRY_DSN=https://your-key@o000000.ingest.sentry.io/0000000
# SENTRY_AUTH_TOKEN=sntrys_your_auth_token

# ============================================
# APP CONFIGURATION
# ============================================
NEXT_PUBLIC_APP_URL=https://outbound-lab.vercel.app
NODE_ENV=development

# ============================================
# RUNTIME CAPS (safety rails)
# ============================================
MAX_RUNS_PER_IP_PER_DAY=10
